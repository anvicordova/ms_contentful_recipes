<% @recipes.each do |recipe| %>
  <div>
    <%= recipe.title %>
    <div>
      <%= image_tag recipe.photo.url, style: 'height:300px;width:auto'%>
    </div>

    <%= link_to("Details", recipe_path(id: recipe.contentful_id)) %>
  </div>
<% end %>